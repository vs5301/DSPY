# Sequence in a sequence

menu = [
    ["fries", "burger"],
    [100, 200, 150]
]

print(len(menu))
print(menu[0], len(menu[0]))
print(menu[1], len(menu[1]))

print(menu[0][1])
print(menu[1][1])

# JSON - JavaScript Object Notation
# Dictionary which can further have list or dictionary or list of dictionary
